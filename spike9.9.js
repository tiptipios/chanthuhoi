  function Buffchiso() {
            
              var searchValue = -2079391149;
     var replaceValue = 6000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
        '0x100000000',
        '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
    }

    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if ( result.address.endsWith('414')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391179;
      replaceValue = 6000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
        '0x100000000',
        '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
    }

    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if ( result.address.endsWith('3CC')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
         searchValue = -2079391159;
      replaceValue = 6000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
        '0x100000000',
        '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
    }

    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if ( result.address.endsWith('3F0')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
});
    searchValue = -2079391644; // Chi so 100
    replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('458')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391629; // Chi so 130
       replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
    if (result.address.endsWith('4E8')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
       searchValue = -2079391614; // Chi so 130
       replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
       if (result.address.endsWith('098') || result.address.endsWith('0BC')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391609; // Chi so 135
       replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('47C') || result.address.endsWith('4DC')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391594; // Chi so 150
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('1A0')|| result.address.endsWith('368') || result.address.endsWith('38C') || result.address.endsWith('3E0')|| result.address.endsWith('3EC') || result.address.endsWith('3F8') || result.address.endsWith('404')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
     searchValue = -2079391589; // Chi so 155
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000')
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('080') || result.address.endsWith('038') || result.address.endsWith('08C') || result.address.endsWith('02C')|| result.address.endsWith('0B0') || result.address.endsWith('284') || result.address.endsWith('2FC')|| result.address.endsWith('548') || result.address.endsWith('554') || result.address.endsWith('560')|| result.address.endsWith('56C') || result.address.endsWith('5C0') ||
            result.address.endsWith('5E4')|| result.address.endsWith('C94')|| result.address.endsWith('CA0') || result.address.endsWith('CAC') ||
            result.address.endsWith('CB8')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });

      
    searchValue = -2079391584; // Chi so 160
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('20C') ||result.address.endsWith('F54') || result.address.endsWith('F3C') || result.address.endsWith('110') || result.address.endsWith('125')|| result.address.endsWith('128') || result.address.endsWith('134') || result.address.endsWith('0AC')|| result.address.endsWith('308') || result.address.endsWith('374') || result.address.endsWith('380')|| result.address.endsWith('644') || result.address.endsWith('650') || result.address.endsWith('65C')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391579; // Chi so 165
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('4F4') ||result.address.endsWith('044') || result.address.endsWith('1AC') || result.address.endsWith('190') || result.address.endsWith('314')|| result.address.endsWith('2F0') || result.address.endsWith('290')|| result.address.endsWith('29C') || result.address.endsWith('194')|| result.address.endsWith('5CC')|| result.address.endsWith('5D8')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391574; // Chi so 170
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('224') || result.address.endsWith('020')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391569; // Chi so 175
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('FFC') || result.address.endsWith('278') || result.address.endsWith('464') || result.address.endsWith('470') || result.address.endsWith('638')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391564; // Chi so 180
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('F48') ||
        result.address.endsWith('188')||
        result.address.endsWith('218')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    
    searchValue = -2079391559; // Chi so 185
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('FA8') || result.address.endsWith('200') || result.address.endsWith('4D0') || result.address.endsWith('638')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    searchValue = -2079391549; // Chi so 195
     replaceValue = 1000;

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
'0x100000000', '0x160000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
        
    }
    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('F30')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });
    
     replaceValue = 1000;

    // Define a new searchValue and repeat the process
    searchValue = -2079391544;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());

    // Iterate over each result and replace the value for those with address ending with "AD0"
    for (var j = 0; j < results.length; j++) {
        var targetResult = results[j];
        // Check if the address ends with "AD0"
        if (targetResult.address.endsWith("0A4")) {
            h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32');
        }
    };
replaceValue = 66280;

    // Define a new searchValue and repeat the process
    searchValue = -2079363595;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());

    // Iterate over each result and replace the value for those with address ending with "AD0"
    for (var j = 0; j < results.length; j++) {
        var targetResult = results[j];
        // Check if the address ends with "AD0"
        if (targetResult.address.endsWith("560")) {
            h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32');
        }
    };
    replaceValue = 66280;

    // Define a new searchValue and repeat the process
    searchValue = 52479;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());

    // Iterate over each result and replace the value for those with address ending with "AD0"
    for (var j = 0; j < results.length; j++) {
        var targetResult = results[j];
        // Check if the address ends with "AD0"
        if (targetResult.address.endsWith("550")) {
            h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32');
        }
    };
   replaceValue = 66280;

    // Define a new searchValue and repeat the process
    searchValue = 6409202;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());

    // Iterate over each result and replace the value for those with address ending with "AD0"
    for (var j = 0; j < results.length; j++) {
        var targetResult = results[j];
        // Check if the address ends with "AD0"
        if (targetResult.address.endsWith("53C")) {
            h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32');
        }
    };
   
    // Alert that the operation is complete
    alert("Đã buff chỉ số 🐧");
       
}

        
        


        var Styleisabel = 16777215; // Biến toàn cục để lưu trữ giá trị cho ModCamCao

        function ModStyleisabel() {
            var searchValue = Styleisabel;
            var replaceValue = Number(prompt("Vui lòng chọn một trong các màu sau:\n  Trắng: 16777215\nXanh dương: 16711680\nXanh lá: 65280\nTím: 8388736\nHồng : 15680245\n\n \nNhập màu mới isb ", ""));

            h5gg.clearResults();
            h5gg.searchNumber(searchValue,
                'I32',
                     '0x110000000',
        '0x116000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length === 0) {
                alert("Không tìm thấy giá trị cần thay thế.");
                return;
            }

            for (var i = 0; i < results.length; i++) {
                // Check if the offset ends with '3e8' or 'ab4'
                if (results[i].address.endsWith('538') || results[i].address.endsWith('46C')|| results[i].address.endsWith('27C')) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
                }
            }

            alert("Đã thực hiện thay thế ");
            Styleisabel = replaceValue;

        }
        var StyleOld = 52479;
        var StyleOlded = 16777215;// Biến toàn cục để lưu trữ giá trị cho ModCamCao

        function ModStyle() {
            var searchValue = StyleOld;
            var replaceValue = Number(prompt("Vui lòng chọn một trong các màu sau:\n  Trắng: 16777215\nXanh dương: 16711680\nXanh lá: 65280\nTím: 8388736\nHồng : 15680245\n\n \nNhập màu mới", ""));

            h5gg.clearResults();
            h5gg.searchNumber(searchValue,
                'I32',
                '0x100000000',
                '0x160000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length === 0) {
                alert("Không tìm thấy giá trị cần thay thế.");
                return;
            }

            for (var i = 0; i < results.length; i++) {
                // Check if the offset ends with '3e8' or 'ab4'
                if (results[i].address.endsWith('A20') || results[i].address.endsWith('DB0') ) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
                }
            }
            var searchValue = StyleOlded;
            var replaceValue = Number(prompt("Vui lòng chọn một trong các màu sau:\n  Trắng: 16777215\nXanh dương: 16711680\nXanh lá: 65280\nTím: 8388736\nHồng : 15680245\n\n \nNhập màu tia nhỏ mới", ""));

            h5gg.clearResults();
            h5gg.searchNumber(searchValue,
                'I32',
                '0x110000000',
        '0x116000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length === 0) {
                alert("Không tìm thấy giá trị cần thay thế.");
                return;
            }

            for (var i = 0; i < results.length; i++) {
                // Check if the offset ends with '3e8' or 'ab4'
                if (results[i].address.endsWith('5DC')) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
                }
}
            alert("Đã thực hiện thay thế ");
            StyleOld = replaceValue;

        }
var StyleSiwoo = 16777215; // Biến toàn cục để lưu trữ giá trị cho ModCamCao

function ModSiwoo() {
    var searchValue = StyleSiwoo;
    var replaceValue = Number(prompt("Có thể chọn một trong các màu sau:\n Đen: 1 \n  Trắng: 16777215\nXanh dương: 16711680\nXanh lá: 65280\nTím: 8388736\nHồng : 15680245 \nĐỏ: 983543 \n \nNhập màu mới", ""));

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
        '0x110000000',
        '0x116000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
    }

    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('D4C') || result.address.endsWith('F14') || result.address.endsWith('F68')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });

    alert("Đã thực hiện thay thế ");
    StyleSiwoo = replaceValue;
}
 StyleNgam = 16777215; // Biến toàn cục để lưu trữ giá trị cho ModCamCao

function ModNgam() {
    var searchValue = StyleNgam;
    var replaceValue = Number(prompt("Có thể chọn một trong các màu sau:\n Đen: 1 \n  Trắng: 16777215\nXanh dương: 16711680\nXanh lá: 65280\nTím: 8388736\nHồng :15680245 \nĐỏ: 983543 \n \nNhập màu mới", ""));

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
        '0x110000000',
        '0x116000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
    }

    results.forEach(result => {
        // Check if the offset ends with '54C' or 'EC4'
        if (result.address.endsWith('5E4') || result.address.endsWith('554') || result.address.endsWith('4C4') ||
        result.address.endsWith('434')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });

    alert("Đã thực hiện thay thế ");
    StyleNgam = replaceValue;
}
 var StyleControll = 16777215; // Biến toàn cục để lưu trữ giá trị cho ModCamCao

function ModControll() {
    var searchValue = StyleControll;
    var replaceValue = Number(prompt("Có Thể chọn một trong các màu sau:\n Đen: 1\n  Trắng: 16777215\nXanh dương: 16711680\nXanh lá: 65280\nTím: 8388736\nHồng : 15680245 \nĐỏ: 983543 \n \nNhập màu mới", ""));

    h5gg.clearResults();
    h5gg.searchNumber(searchValue,
        'I32',
        '0x110000000',
        '0x116000000');
    var results = h5gg.getResults(h5gg.getResultsCount());

    if (results.length === 0) {
        alert("Không tìm thấy giá trị cần thay thế.");
        return;
    }

    results.forEach(result => {
        // Check if the offset ends with specific suffixes
        if (result.address.endsWith('E54') || result.address.endsWith('F18') || result.address.endsWith('E94') || result.address.endsWith('FBC') || result.address.endsWith('170') ||
            result.address.endsWith('A3C') || result.address.endsWith('B5C') || result.address.endsWith('610') || result.address.endsWith('188') || result.address.endsWith('288')||result.address.endsWith('294')|| result.address.endsWith('19C')) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
        }
    });

    alert("Đã thực hiện thay thế ");
    StyleControll = replaceValue;
}
function RollS() {
 // Reset replaceValue for the next operation


    // Define the search and replace values
     searchValue = -2079391645;
     replaceValue = 2;

    // Clear any previous search results
    h5gg.clearResults();

    // Search for the searchValue in the specified memory range
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    
    // Get the search results
    var results = h5gg.getResults(h5gg.getResultsCount());

    // Check if results were found
    if (results.length > 0) {
        // Iterate over each result and replace the value
        for (var i = 0; i < results.length; i++) {
            var targetResult = results[i];
            h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32');
        }
        // Alert that the operation is complete
        alert("Đã Có thể Roll S!");
    } else {
        // Alert if no suitable results were found
        alert("Không tìm thấy kết quả phù hợp để thay đổi.");
  
}}
        
             
        
var YongsubValue = -2079391372;


function modifyValue(searchValue, replaceValue, addressEndsWith, alertMessage) {
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    const results = h5gg.getResults(h5gg.getResultsCount());
    const targetResult = results.find(result => result.address.endsWith(addressEndsWith));
    if (targetResult) {
        h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32');
        if (alertMessage) alert(alertMessage);
        if (searchValue === YongsubValue) YongsubValue = replaceValue;
    } else if (alertMessage) {
        alert("Không tìm thấy kết quả phù hợp để thay đổi.");
    }
}
function ModNishi() {
    modifyValue(YongsubValue, -2079391594,"820");
    alert("Roll Nishikawa S+ Đi Bro.");
}

function ModYamedera() {
    modifyValue(YongsubValue, -2079388562,"820");
    alert("Roll Yamedera S Đi Bro.");
}

function ModSohee() {
    modifyValue(YongsubValue, -2079389760,"820");
    alert("Roll Sohee S Đi Bro.");
}


function SkillIsabel() {
    modifyValue(-2079391744,100,"D34");
    modifyValue(-2079391644, 0, "D00");
    modifyValue(-2079391644, 0, "D68");
    alert(" Đã mod Isabel ult.");
}
function SanghyeonULT() {
    modifyValue(-2079391444,9999,"6F8");
    modifyValue(-2079391444,9999,"C88");
    alert(" Đã mod Sanghyeon ult .");
}
function SkillYamadera() {
    modifyValue(-2079391504,9999,"540");
    alert(" Đã mod Yamedera ult .");
}

        var camCaoValue = -2079391494; // Biến toàn cục để lưu trữ giá trị cho ModCamCao

        function ModCamCao() {
            var searchValue = camCaoValue;
            var replaceValue = Number(prompt("Nhập số mới dưới  ", ""));

            h5gg.clearResults();
            h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            var targetResult = results.find(result => result.address.endsWith("788")); // Tìm kết quả có offset kết thúc bằng 990

            if (targetResult) {
                h5gg.setValue(targetResult.address, replaceValue.toString(), 'I32'); // Thay đổi giá trị tìm được
                camCaoValue = replaceValue; // Lưu giá trị đã thay đổi vào biến toàn cục
                alert("Mod Cam Cao thành công");
            } else {
                alert(" Lỗi rồi thoát game đi ");
            }
        }
        
      
        
        function readI32(addr) {
            return Number(h5gg.getValue(addr, "I32"));
        }

        function searchAndReplace(searchValue, replaceValue, valueType, offset) {
            h5gg.clearResults();
            h5gg.searchNumber(searchValue, valueType, '0x100000000', '0x160000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length === 0) {
                alert("Không tìm thấy giá trị cần thay thế.");
                return false;
            }

            for (var i = 0; i < results.length; i++) {
                if (results[i].address.endsWith(offset)) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), valueType);
                    return true;
                }
            }
            return false;
        }
        // Define a global variable to store the original value



        function Makei32() {
            var searchValue = Number(prompt("Nhập giá trị cần tìm kiếm i4 ", ""));
            var replaceValue = Number(prompt("Nhập giá trị mới ", ""));

            h5gg.clearResults();
            h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length > 0) {
                // Nếu tìm thấy kết quả, lặp qua từng kết quả và thực hiện thay đổi
                for (var i = 0; i < results.length; i++) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), 'I32');
                }
                alert(" OK Rồi Bú Đi  ");
            } else {
                alert("Lỗi rồi thoát game đi");
            }
        }

        function Makef64() {
            var searchValue = Number(prompt("Nhập giá trị cần tìm kiếm d ", ""));
            var replaceValue = Number(prompt("Nhập giá trị mới ", ""));

            h5gg.clearResults();
            h5gg.searchNumber(searchValue, 'F64', '0x100000000', '0x160000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length > 0) {
                // Nếu tìm thấy kết quả, lặp qua từng kết quả và thực hiện thay đổi
                for (var i = 0; i < results.length; i++) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), 'F64');
                }
                alert(" OK Rồi Bú Đi  ");
            } else {
                alert("Lỗi rồi thoát game đi");
            }
        }

        
function ModNam() {
            var searchValue = -2079391714;
            var replaceValue = -2079391644;

            h5gg.clearResults();
            h5gg.searchNumber(searchValue,
                'I32',
                '0x100000000',
                '0x160000000');
            var results = h5gg.getResults(h5gg.getResultsCount());

            if (results.length === 0) {
                alert("Không tìm thấy giá trị cần thay thế.");
                return;
            }

            for (var i = 0; i < results.length; i++) {
                // Check if the offset ends with '3e8' or 'ab4'
                if (results[i].address.endsWith('3C4') || results[i].address.endsWith('014')) {
                    h5gg.setValue(results[i].address, replaceValue.toString(), 'I32'); // Specifying 'I32' as the data type
                }
            }

            alert(" Nam ULT ");

        }
function Siwoobeak() {
    var searchValue, replaceValue, results, targetResult;

    // Bước 1: Tìm và thay thế YongsubValue
    searchValue = 357696256;
    replaceValue = 357696255;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());
    results.forEach(result => {
        if (result.address.endsWith("DE0") || result.address.endsWith("B44")) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32');
            // Cập nhật YongsubValue với replaceValue
        }
    });

    // Bước 3: Tìm và thay thế giá trị khác
    replaceValue = -2079390644;

    searchValue = -2079391662;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());
    results.forEach(result => {
        if (result.address.endsWith("DDC") || result.address.endsWith("B40")) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32');
        }
    });

    alert("Đã Mod Siwoo beak.");
}
function Matday() {
    var searchValue, replaceValue, results, targetResult;

    // Bước 1: Tìm và thay thế YongsubValue
    
    // Bước 3: Tìm và thay thế giá trị khác
    replaceValue = -2079390644;

    searchValue = -2079391662;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());
    results.forEach(result => {
        if (result.address.endsWith("59C")) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32');
            Buoc1 = replaceValue;
        }
    });

    alert(" Phát Bóng Mất Dạy.");
}
function Phatbongcao() {
    var searchValue, replaceValue, results, targetResult;

    // Bước 1: Tìm và thay thế YongsubValue
    
    // Bước 3: Tìm và thay thế giá trị khác
    replaceValue = Number(prompt("Nhập độ cao", ""));

    searchValue = -2079391684;
    h5gg.clearResults();
    h5gg.searchNumber(searchValue, 'I32', '0x100000000', '0x160000000');
    results = h5gg.getResults(h5gg.getResultsCount());
    results.forEach(result => {
        if (result.address.endsWith("8A8")) {
            h5gg.setValue(result.address, replaceValue.toString(), 'I32');
            Buoc1 = replaceValue;
        }
    });

    alert("đã bật Phát Bóng Cao.");
}